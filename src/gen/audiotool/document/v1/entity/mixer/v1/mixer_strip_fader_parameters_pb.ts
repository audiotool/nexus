// @generated by protoc-gen-es v1.10.0 with parameter "target=ts"
// @generated from file audiotool/document/v1/entity/mixer/v1/mixer_strip_fader_parameters.proto (package audiotool.document.v1.entity.mixer.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import type { BinaryReadOptions, FieldList, JsonReadOptions, JsonValue, PartialMessage, PlainMessage } from "@bufbuild/protobuf";
import { Message, proto3 } from "@bufbuild/protobuf";

/**
 * Parameters of faders of channel, aux and group strips, controlling panning, gain, and solo/mute state.
 * This is not an entity but a submessage used by these strips.
 *
 * @generated from message audiotool.document.v1.entity.mixer.v1.MixerStripFaderParameters
 */
export class MixerStripFaderParameters extends Message<MixerStripFaderParameters> {
  /**
   * Panning of the strip. -1 is fully left, 0 is centered (unchanged), 1 is fully right.
   *
   * @generated from field: float panning = 1;
   */
  panning = 0;

  /**
   * Gain right before the signal leaves the strip, i.e. after inserts,
   * eq, aux sends, etc., Equivalent to a dB range of [-inf, 6.0].
   *
   * @generated from field: float post_gain = 2;
   */
  postGain = 0;

  /**
   * If is_muted=true, the strip is muted.
   *
   * If a channel, group or aux strip has "is_muted" set to true any signal is cut off in the strip before it's sent to any inserts, effects,
   * or aux strips. If a channel or group that has "is_muted" set to true is part of a group in some way,
   * all descendants of the channel in the group hierarchy also behave as though they had is_muted=true.
   *
   * @generated from field: bool is_muted = 3;
   */
  isMuted = false;

  /**
   * If is_soloed=true, the strip is soloed. The behavior depends on the type of strip:
   *
   * - Channel and group strips:
   *   If a channel or group strip has is_soloed true, all other channel and group strips behave as though
   *   they have is_muted=true, with the exception of ancestors and descendants of the soloed strip in the group hierarchy, which behave as
   *   though they have is_soloed=true as well. No effect on aux strips themselves.
   *
   * - Aux strips:
   *   If an aux strip has is_soloed set to true, all other aux strips behave as though they had is_muted=true. Further, channel
   *   and group strips whose output is routed directly to the master behave as though their fader gain are set to 0. This
   *   way, none of them are audible, but aux sends of both channels and groups still get passed through.
   *
   *   The solo and mute behavior of channel and group strips override the solo/mute behavior of the
   *   aux strips - if a channel is muted, it doesn't send to any aux anymore; if a channel is soloed,
   *   it becomes audible.
   *
   *
   * @generated from field: bool is_soloed = 4;
   */
  isSoloed = false;

  constructor(data?: PartialMessage<MixerStripFaderParameters>) {
    super();
    proto3.util.initPartial(data, this);
  }

  static readonly runtime: typeof proto3 = proto3;
  static readonly typeName = "audiotool.document.v1.entity.mixer.v1.MixerStripFaderParameters";
  static readonly fields: FieldList = proto3.util.newFieldList(() => [
    { no: 1, name: "panning", kind: "scalar", T: 2 /* ScalarType.FLOAT */ },
    { no: 2, name: "post_gain", kind: "scalar", T: 2 /* ScalarType.FLOAT */ },
    { no: 3, name: "is_muted", kind: "scalar", T: 8 /* ScalarType.BOOL */ },
    { no: 4, name: "is_soloed", kind: "scalar", T: 8 /* ScalarType.BOOL */ },
  ]);

  static fromBinary(bytes: Uint8Array, options?: Partial<BinaryReadOptions>): MixerStripFaderParameters {
    return new MixerStripFaderParameters().fromBinary(bytes, options);
  }

  static fromJson(jsonValue: JsonValue, options?: Partial<JsonReadOptions>): MixerStripFaderParameters {
    return new MixerStripFaderParameters().fromJson(jsonValue, options);
  }

  static fromJsonString(jsonString: string, options?: Partial<JsonReadOptions>): MixerStripFaderParameters {
    return new MixerStripFaderParameters().fromJsonString(jsonString, options);
  }

  static equals(a: MixerStripFaderParameters | PlainMessage<MixerStripFaderParameters> | undefined, b: MixerStripFaderParameters | PlainMessage<MixerStripFaderParameters> | undefined): boolean {
    return proto3.util.equals(MixerStripFaderParameters, a, b);
  }
}

