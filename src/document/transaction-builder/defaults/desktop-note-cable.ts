import type { DesktopNoteCableConstructor } from "@gen/document/v1/entity/desktop_note_cable/v1/desktop_note_cable_nexus"
import type { Defaults } from "./default-type"

export const desktopNoteCableDefaults: Defaults<DesktopNoteCableConstructor> = {
  colorIndex: 0,
}
